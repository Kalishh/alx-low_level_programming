#include "main.h"
#include <stdio.h>

/**
 * _isdigit - is the function used
 * @c: is the variable used
 * Return: 0 and 1
 */

int _isdigit(int c)
{
	for(c = 0; c <= 9; c++)
{
	printf ("%d\n", c);
	return (1);
}
return (0);
}
