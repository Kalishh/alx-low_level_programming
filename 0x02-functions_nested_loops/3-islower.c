#include "main.h"

/**
 * main - check the code.
 * Return: Always 0.
 */

int main(void)

{
     int r;
     if (r >= 'a' && r <= 'z')
     {
     return (1);
     }
     else
     {
     return (0);
     }
}
