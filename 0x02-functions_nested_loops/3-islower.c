#include "main.h"

/**
 * _islower - check the code.
 * Return: Always 0.
 */

int _islower(int c)
{
     int r;
     if (r >= 'a' && r <= 'z')
     {
     return (1);
     }
     else
     {
     return (0);
     }
}
